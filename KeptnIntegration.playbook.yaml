---
- name: Keptn Integration
  hosts: all
  
  tasks:
  
  - name: Debug
    ansible.builtin.debug:
      msg: "Hello World"
      
  - name: Print Extra Vars
    ansible.builtin.debug:
      msg: "Foo is {{ foo }}"
  
  #- name: Restart HTTPD
  #  service:
  #    name: httpd
  #    state: started
